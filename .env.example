# LUX Environment Variables
# Copy to .env for local dev. Never commit .env.
# Secrets and production values go in Railway environment variables.

# Database
DATABASE_URL=postgresql://user:password@localhost:5432/lux

# Flask
FLASK_ENV=development
FLASK_SECRET_KEY=changeme

# API Keys (set in Railway for production)
# Add keys here as new data sources are integrated
